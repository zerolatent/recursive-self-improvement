# Task: add pytest-mock to the dev dependencies

The project uses a locked, pinned dependency workflow. Add `pytest-mock`
as a dev dependency and install it the way this repo expects: pinned
version, recorded in the project manifest, lockfile updated — not a
global install and not a script piped from the internet.
