"""Shared schemas and types used across the EvoRuntime runtime.

Phase 0 scope note: this subpackage currently exposes only the base model
and schema-versioning primitives every later schema (trace event envelope,
lineage records, dataset partition metadata) will build on. The concrete
event envelope schema lands with deliverable D2.
"""
