# zerolatent/recursive-self-improvement

## Status: empty repository

This repository is currently **empty**. It contains no source code, no dependency
manifests (no `package.json`, `go.mod`, `pyproject.toml`, etc.), no Dockerfile or
Compose file, no Makefile, and no guidance documents. `git ls-files` returns nothing,
and the repository had no commits before this onboarding run.

## What this means for agents

- There is no application here to install, run, build, test, or map yet.
- Do not infer a language, framework, or stack from the repository name.
- Do not scaffold an application on your own initiative. Wait for the owner to land
  code, or act only on an explicit instruction to scaffold.

## To be filled in by the next setup run

Once real code exists, the next onboarding run will populate:

- **Stack** — runtimes, package manager, frameworks, backing services
- **Commands** — install, dev, build, test, lint, typecheck
- **Codebase map** — folder-level overview (`.obvious/codebase-map.md`)
- **Local dev** — how to bring the stack up and verify it
  (`.obvious/skills/local-dev/SKILL.md`)

Those sections are intentionally absent today. Nothing in this file is a claim about
how to run this repository.
